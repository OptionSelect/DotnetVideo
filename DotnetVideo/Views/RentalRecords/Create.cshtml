@model DotnetVideo.Models.CheckoutViewModel 
@{ ViewData["Title"] = "Create"; }

<h2>Rent a Movie</h2>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-controller="RentalRecords" asp-action="CreateRecord">
            <p>
                <label for="movie">Movie:</label><select name="movie">
            @foreach(var item in Model.Movies)
            {
               <option value=@item.MovieId>@item.MovieName</option>
            }
            </select>
            </p>
            <p>
                <label for="customer">Customer:</label><select name="customer">
            @foreach(var item in Model.Customers)
            {
               <option value=@item.CustomerId>@item.CustomerName</option>
            }
            </select>
                <div class="form-group">
                    <label asp-for="RentalDate" class="control-label"></label>
                    <input asp-for="RentalDate" class="form-control" />
                    <span asp-validation-for="RentalDate" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="DueDate" class="control-label"></label>
                    <input asp-for="DueDate" class="form-control" />
                    <span asp-validation-for="DueDate" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-default" />
                </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>